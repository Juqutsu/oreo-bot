# Welle 0: DX-Quick-Wins Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox syntax.

**Goal:** Lokale Dev-MySQL für die 9 DB-gestützten Test-Suiten, CI-Tests für Feature-Branches, und fünf Kleinst-Fixes aus den Review-Runden.

**Architecture:** Keine Produktionslogik-Änderungen außer 0.3 (kleine, reviewte Fixes). Neue Datei `docker-compose.dev.yml` (nur DB), CI-Workflow-Split (Test auf allen Branches, Deploy nur main).

**Tech Stack:** Docker Compose, GitHub Actions, Node/mysql2 wie gehabt.

## Global Constraints

- User-facing Strings deutsch, Code/Kommentare englisch; CommonJS; keine neuen npm-Dependencies.
- Produktions-`docker-compose.yml` und Live-`.env` NICHT anfassen.
- Branch: `chore/wave0-dx` von `main`.
- Commit pro Task.

---

### Task 1: Lokale Dev-DB + CI für Branches

**Files:**
- Create: `docker-compose.dev.yml`
- Modify: `package.json` (scripts)
- Modify: `.github/workflows/deploy.yml`
- Modify: `README.md` (kurzer Abschnitt "Tests lokal ausführen")

**Steps:**

- [ ] `docker-compose.dev.yml` — NUR ein MySQL-Service, Creds identisch zur CI (`deploy.yml` lesen): root `root_test_pass`, DB `oreo_test`, User `oreo_test`/`oreo_test_pass`, Port-Mapping `3310:3306` (3306 könnte lokal belegt sein), Healthcheck `mysqladmin ping`, benanntes Volume `oreo_test_db`.
- [ ] `package.json`: Script `"test:local": "docker compose -f docker-compose.dev.yml up -d --wait && cross-env-frei per Env-Präfix testen"` — ACHTUNG kein cross-env (keine neuen Deps): Windows-kompatibel lösen, z. B. `test:local` startet nur die DB (`docker compose -f docker-compose.dev.yml up -d --wait`) und ein zweites Script/README-Zeile dokumentiert den Testlauf: Node kann Env inline nicht cross-platform — stattdessen liest `tests/run.js` bereits `.env`; Lösung: `.env.test` mit den Dev-DB-Werten (MYSQL_HOST=127.0.0.1, MYSQL_PORT=3310, User/Pass/DB wie oben, Dummy DISCORD_TOKEN/CLIENT_ID/GUILD_ID wie CI) einchecken und `test:local` = `docker compose -f docker-compose.dev.yml up -d --wait && node --env-file=.env.test tests/run.js` (Node >= 20.6 hat --env-file; prüfen wie tests/run.js Env liest — wenn es process.env nutzt, reicht --env-file). `.env.test` enthält NUR Test-Creds, keine Secrets — committen ist ok.
- [ ] `deploy.yml`: Test-Job zusätzlich auf `push: branches: ['**']` (oder `pull_request`) triggern; Deploy-Job bekommt `if: github.ref == 'refs/heads/main'` + bleibt `needs: test`. Keine doppelten Läufe auf main erzeugen (ein `push`-Trigger mit branches: ['**'] reicht, Deploy-if regelt den Rest).
- [ ] README: 5-Zeilen-Abschnitt „Tests lokal: `npm run test:local` (braucht Docker Desktop)".
- [ ] Verifizieren: `docker compose -f docker-compose.dev.yml config -q`; wenn Docker-Daemon läuft: DB hochfahren und `npm run test:local` → alle 16 Suiten grün (erster echter Lokal-Volllauf!). YAML-Lint des Workflows via `node -e` YAML-parse entfällt (kein Dep) — sorgfältig manuell prüfen.
- [ ] Commit: `chore: local dev database and branch CI for full test runs`

### Task 2: Review-Kleinst-Fixes

**Files:** `src/interactions/announcement.js`, `src/config.js`, `tests/smoke/join_role.js`, `tests/smoke/server_logging.js`, `tests/smoke/announcement_flow.js`

**Steps:**

- [ ] `delconfirm`-Claim: in `handleDelConfirm` (yes-Zweig) synchronen Claim analog `session.posting` — hier gibt es keine Session; stattdessen Modul-Map `pendingDeletes = new Set<announcementId>`: `if (pendingDeletes.has(id)) { reply '⏳ Wird bereits gelöscht …' ephemeral; return true; } pendingDeletes.add(id);` mit `finally { pendingDeletes.delete(id); }`.
- [ ] `try/finally` um die posting-Flag-Nutzung: `handlePreviewButton` post-Zweig ruft handler in `try { … } finally { if (previewSessions.has(nonce)) session.posting = false; }` — Semantik erhalten: Erfolgspfad löscht die Session (dann ist das finally-Reset ein No-Op), Fehlerpfade werden vom finally abgedeckt; die expliziten `posting = false`-Resets in den Handlern können danach entfallen (vereinfachen!).
- [ ] Config-Cache-Coalescing: in `readGuildRow` (und `getBadWords`-Cache-Fill) laufende Fetch-Promises in einer Map `inflight` dedupen: `if (inflight.has(guildId)) return inflight.get(guildId);` … `finally { inflight.delete(guildId); }`.
- [ ] Test-Mocks: in `tests/smoke/join_role.js` und `tests/smoke/server_logging.js` den `roles.cache`-Mocks eine `find`-Methode geben (`find: () => undefined`), damit der Mute-Rejoin-Pfad keinen gefangenen TypeError mehr loggt. Tests müssen weiter grün sein.
- [ ] `truncateForDiff` in `_internal` exportieren + 2 Assertions in `announcement_flow.js` (301-Zeichen-String → 300 + '…'-Verhalten prüfen — exakte Semantik aus dem Code lesen).
- [ ] Verifizieren: volle lokale Testbatterie (nach Task 1 möglich): `npm run test:local` → 16/16 grün.
- [ ] Commit: `fix: delete-claim, posting try/finally, cache coalescing, test mock cleanup`

### Task 3: Merge

- [ ] Final-Check + Merge `chore/wave0-dx` → `main`, Push (CI: Test → Deploy). Roadmap-Commit `2541726` wird mitgepusht.
